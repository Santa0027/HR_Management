# Dockerfile.frontend
FROM node:lts
 # Confirmed 'lts' is correct as per your logs

# Force rebuild: Added comment to invalidate cache (2025-06-25)
WORKDIR /app

# Corrected COPY path: Copies package.json from the 'frontend' subdirectory
COPY frontend/package*.json ./
RUN npm install

# Corrected COPY path: Copies all other frontend files from the 'frontend' subdirectory
COPY frontend/ .

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

